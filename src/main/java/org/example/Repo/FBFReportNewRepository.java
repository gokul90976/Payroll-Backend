package org.example.Repo;

import org.example.Entity.FBFReportNew;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FBFReportNewRepository extends JpaRepository<FBFReportNew, Long> {
}