package org.example.Repo;

import org.example.Entity.NhisReportNew;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NhisReportNewRepository extends JpaRepository<NhisReportNew, Long> {
}